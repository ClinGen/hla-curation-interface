[Unit]
Description=daphne service
Requires=daphne.socket
After=network.target

[Service]
Type=simple
User={{ ansible_user }}
WorkingDirectory={{ working_dir }}
ExecStart={{ venv_dir }}/bin/daphne -b 0.0.0.0 -p 8000 config.asgi:application

[Install]
WantedBy=multi-user.target